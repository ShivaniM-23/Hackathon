import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  async rewrites() {
    return [
      {
        source: '/api/:path*',
        destination: 'http://localhost:8000/api/:path*',
      },
    ]
  },
  allowedDevOrigins: ['localhost:8000', 'localhost:3000']
};

export default nextConfig;
